# Pre-specified synthetic sensitivities

Only final sensitivity layers used in the manuscript are retained:

- `backbone/`: Ridge-to-GBR backbone sensitivity; the refactored script reproduces the archived result tables exactly from frozen primary results.
- `thresholds/`: operational coverage/ESS/usability threshold sensitivity; the refactored script reproduces the archived tables exactly.
- `coverage_levels/`: nominal coverage 0.80/0.95 configurations and archived outputs.
- `heavy_tail/`: unit-variance Student-t3 outcome-noise configuration and archived outputs.

No pilot or estimator-screen artifacts are included.
